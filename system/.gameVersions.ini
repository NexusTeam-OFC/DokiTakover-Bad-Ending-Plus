local gameVersion = Revision A, Revision B, Revision C
local curVersion = Revision C

If curVersion is == Revision A then
disable.bversion
disable.cversion

If curVersion is == Revision B then
aversion.disable
cversion.disable

If curVersion is == Revision C then
disable A.version
disable B.version
    end
end
[
],
Currently Version = C

> disable

remove all version except C.version
    end
end